# Server
PORT=3000
HOST=0.0.0.0
LOG_LEVEL=info

# Database
# docker-compose maps the container to host port 5433
DATABASE_URL=postgres://tenet:tenet@localhost:5433/tenet_audit

# Rate limiting (requests per minute)
RATE_LIMIT_AUTHENTICATED=600
RATE_LIMIT_ANONYMOUS=60
